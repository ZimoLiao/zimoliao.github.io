{
  "name": "zimoliao.github.io",
  "private": true,
  "scripts": {
    "check": "npm run format:check && npm test",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "test": "node --test assets/js/__tests__/*.test.js"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "1.4.0",
    "prettier": "3.1.1"
  }
}
